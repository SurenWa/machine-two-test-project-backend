let express,mongoose,cors,readdirSync;_46d‍.w("express",[["default",["express"],function(v){express=v}]]);_46d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_46d‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_46d‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);




const morgan = require("morgan");
require("dotenv").config();

const app = express();

// db
mongoose
    .connect(process.env.DATABASE)
    .then(() => console.log("DB Connected"))
    .catch((err) => _46d‍.g.console.log("DB Connection Error ", err));

// middlewares
app.use(express.json({ limit: "5mb" }));
app.use(
    cors({
        origin: [process.env.CLIENT_URL],
    })
);

// autoload routes
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// listen
const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));

