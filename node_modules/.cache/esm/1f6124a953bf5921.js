let User,Post;_53b‍.x([["addPost",()=>addPost]]);_53b‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_53b‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);


       const addPost = async (req, res) => {
    try {
        const {_id} = req.user;
        //console.log(_id)
        const { title, description } = req.body;
        if(!title || !description) {
            return res.status(400).send("Title and description is required")
        }
        const post = await new Post({
            createdBy: _id,
            title,
            description
        }).save()
        return res.json({
            post
        })
        
    } catch (error) {
        _53b‍.g.console.log(error)
        res.status(400).send("Post creation failed");
    }
}